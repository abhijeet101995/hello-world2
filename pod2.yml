apiVersion: v1
kind: ReplicationController

spec :
  tepmlate:
    replicas: 3
  metadata:
    name: nginx-pod
  labels:
    app: nginx

spec:
  containers:
    - name: nginx
      image: nginx
      ports:
        - containerPort: 80
 selector:
   matchlabels:
     app: nginx
 
